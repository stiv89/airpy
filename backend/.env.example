PORT=3000
# Orígenes explícitos (opcional). También se permiten los dominios de ALLOWED_DOMAINS.
CLIENT_ORIGIN=http://localhost:4200,https://www.viadrop.lat,https://viadrop.lat
# Dominios permitidos: viadrop.lat acepta www.viadrop.lat, viadrop.lat, etc.
ALLOWED_DOMAINS=viadrop.lat
# Pon false solo si quieres bloquear previews de Vercel
ALLOW_VERCEL=true
